%h2
  ="#{@master_scenario.name}"

%p 
  = link_to 'Back', admins_master_scenarios_path

%hr

- form_for(@master_scenario, :url => admins_master_scenario_path(@master_scenario), :method => :put) do |f|
  = render :partial => 'form', :locals => { :f => f }

