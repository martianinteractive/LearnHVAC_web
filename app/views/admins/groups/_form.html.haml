= f.error_messages
.clearfix
  = f.label :name
  .input= f.text_field :name, :class => 'xxlarge'

.clearfix
  = f.label :creator
  .input= f.select :creator_id, User.admin_instructor.collect {|user| [ user.name, user.id ] }, { :prompt => 'Please select ...'}, :class => 'xxlarge'

.clearfix
  = f.label :goal
  .input= f.text_area :goal, :rows => '3', :class => 'xxlarge'

.clearfix
  = f.label :scenarios
  %br
  .input= select_tag "group[scenario_ids][]", options_from_collection_for_select(@created_scenarios, :id, :name, @group.scenario_ids.map(&:to_i) || []), :multiple => true, :class => 'xxlarge'
