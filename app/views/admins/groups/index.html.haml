%h2 Listing Groups
%p
  = link_to 'Add a new group', new_admins_group_path
%hr

%table
  %tr
    %th Name
    %th Instructor
    %th Students
  - @groups.each do |group|
    %tr
      %td= link_to group.name, admins_group_path(group)
      %td= group.instructor.name if group.instructor
      %td= group.students.count
%br
= will_paginate @groups

