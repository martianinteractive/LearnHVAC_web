%h2= "Editing #{@institution.name.capitalize}" 
%hr

- form_for(@institution, :url => admin_institution_path(@institution), :method => :put) do |f|
  = render :partial => "form", :locals => { :f => f }
  .actions
    = submit_tag "Update"
    &nbsp;or&nbsp;
    = link_to 'Cancel', :back