%h2= "Editing #{@group.name.titleize}"
%p= link_to "Back", instructors_group_path(@group)
%hr

- form_for(@group, :url => instructors_group_path(@group), :method => :group) do |f|
  = render :partial => "form", :locals => { :f => f }