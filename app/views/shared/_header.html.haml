#header
  %div.welcome
    - if current_user != nil
      %b
        Learn HVAC :
        = "Martian Interactive"# current_user.institution.name
      %br
      Welcome,
      = current_user.first_name
      = current_user.last_name
  %a{ :href => "/account/welcome" }
    %img{ :src => "/images/header_bar.png", :alt => "Learn HVAC logo" }
